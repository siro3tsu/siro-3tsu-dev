---
import Layout from './Layout.astro';
const { frontmatter } = Astro.props;
---

<Layout title={frontmatter.title} useDefaultTitle={false}>
  <article class="prose dark:prose-invert max-w-none">
    <h1>{frontmatter.title}</h1>
    <slot />
  </article>
</Layout>
